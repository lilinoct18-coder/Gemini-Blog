FROM node:20-alpine

WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .

# In dev mode, we build at container start so Ghost is already available.
# After building, we serve with a lightweight static server.
RUN npm install -g serve

EXPOSE 3000

CMD ["sh", "-c", "npm run build && serve dist -l 3000 -s"]
