# Optional: URLs for the two blog links on the landing portal.
# Used at build time. If unset, defaults to localhost with ports 4322 (human) and 4323 (ai).
# Production example: https://human.your-domain.com, https://ai.your-domain.com
# PUBLIC_HUMAN_BLOG_URL=http://localhost:4322
# PUBLIC_AI_BLOG_URL=http://localhost:4323
